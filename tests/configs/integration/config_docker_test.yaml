database:
  username: "TESTUSER"
  password: "testpassword"
  host: "localhost"
  port: 1521
  service: "FREEPDB1"

export:
  schema: "TESTUSER"
  output_dir: "./export_docker"
  parallel: 20
  enable_row_hash: false # --skip-row-hash
  # Note: chunk-size-mb is currently hardcoded to 1024MB (1GB) in Rust.
  # If tables are < 1GB, they will be exported as single files.
